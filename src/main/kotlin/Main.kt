import gui.ShiftPokerApplication

/**
 * Main entry point that starts the [ShiftPokerApplication]
 *
 * Once the application is closed, it prints a message indicating the end of the application.
 */
fun main() {
    ShiftPokerApplication().show()
    println("Application ended. Goodbye")
}